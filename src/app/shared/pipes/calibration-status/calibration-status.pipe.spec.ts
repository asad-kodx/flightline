import { CalibrationStatusPipe } from './calibration-status.pipe';

describe('CalibrationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CalibrationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
