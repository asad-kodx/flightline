import { DeviceIconPipe } from './device-icon.pipe';

describe('DeviceIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceIconPipe();
    expect(pipe).toBeTruthy();
  });
});
