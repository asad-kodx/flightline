import { SensorDisplayIconPipe } from './sensor-icon-pipe';

describe('SensorIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorDisplayIconPipe();
    expect(pipe).toBeTruthy();
  });
});
