import { LiveValuePipe } from './live-value.pipe';

describe('LiveValuePipe', () => {
  it('create an instance', () => {
    const pipe = new LiveValuePipe();
    expect(pipe).toBeTruthy();
  });
});
