import { ControlStatusPipe } from './control-status.pipe';

describe('ControlStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
